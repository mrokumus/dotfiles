[ids]
*

[main]
# CapsLock → tap: Esc, hold: Ctrl
capslock = overload(control, esc)

# ISO key (between left shift and z) → tilde
102nd = `

# Right Ctrl + hjkl → arrow keys
[rightcontrol]
h = left
j = down
k = up
l = right
